# Local-only persistence. Non-local URIs are rejected by app/db.py.
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DATABASE=phi_scanner

# Optional GitHub code-search credential. The tenant-site crawler needs no API key.
# GitHub code search: https://docs.github.com/rest/search/search#search-code
GITHUB_TOKEN=
